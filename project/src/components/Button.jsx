function Button(props) { 
    return (
        const dattour = () => {
            alert(`Ban da dat tour: ${props.label}`);
        }
        return (
            <button onClick={dat} className="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">
                {props.label}
            </button>
        )
}
export default Button;
